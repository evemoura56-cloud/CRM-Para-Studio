# INKHOUSE CRM - Backend Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=inkhouse_crm

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Stripe (Simulado)
STRIPE_SECRET_KEY=sk_test_simulated
STRIPE_WEBHOOK_SECRET=whsec_simulated

# PagSeguro (Simulado)
PAGSEGURO_EMAIL=contato@inkhouse.com
PAGSEGURO_TOKEN=simulated_token
